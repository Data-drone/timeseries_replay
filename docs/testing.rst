Testing
=======

This package leverages `docker`, `docker-compose` and `pytest` for testing.
The docker file in the docker folder and the docker compose script under docker_compose
setup the needed containers in order to run the tests
